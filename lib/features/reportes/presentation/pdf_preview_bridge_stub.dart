import 'dart:typed_data';

import 'package:flutter/material.dart';

Widget buildWebPdfPreview(Uint8List bytes, {Key? key}) {
  return const Center(
    child: Text('La previsualizacion web no esta disponible en esta plataforma.'),
  );
}